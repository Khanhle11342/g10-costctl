# Reflections

1. Em đã học được cách tự động hóa việc quản lý tài nguyên AWS bằng boto3 thông qua các API của EC2, S3, RDS và EBS.

2. Em hiểu rõ hơn cách xây dựng một Python CLI tool và cách đọc unit test như một bản đặc tả (specification) để implement chức năng.

3. Em học được tầm quan trọng của các cơ chế an toàn khi thao tác với tài nguyên AWS như confirmation prompt và dry-run mode trước khi xóa tài nguyên.